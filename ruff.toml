line-length = 120
target-version = "py310"
exclude = [
    ".git",
    "__pycache__",
    "logs",
    "test/logs",
]

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "E501",  # 长行由 line-length 控制
]
